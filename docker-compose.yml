services:
  mysql:
    image: mysql:8.0
    container_name: vm-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: video_mind
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - vm-net
    healthcheck:
      test: ["CMD", "mysql", "-uroot", "-p123456", "-e", "SELECT 1 FROM information_schema.schemata WHERE schema_name = 'nacos';"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 30s

  redis:
    image: redis:7-alpine
    container_name: vm-redis
    ports:
      - "6379:6379"
    networks:
      - vm-net

  rabbitmq:
    image: rabbitmq:4-management
    container_name: vm-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: 123456
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - vm-net

  qdrant:
    image: qdrant/qdrant:latest
    container_name: vm-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - vm-net

  video-mind-parser:
    build: ./video-mind-agent
    container_name: vm-parser
    command: python fetch_video.py
    ports:
      - "8001:8001"
    networks:
      - vm-net

  video-mind-agent:
    build: ./video-mind-agent
    container_name: vm-agent
    command: python web_server.py
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - ANTHROPIC_AUTH_TOKEN=${ANTHROPIC_AUTH_TOKEN}
      - ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL}
      - EMBEDDING_API_KEY=${EMBEDDING_API_KEY}
      - EMBEDDING_BASE_URL=${EMBEDDING_BASE_URL}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL}
      - EMBEDDING_DIMENSIONS=${EMBEDDING_DIMENSIONS:-4096}
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
    ports:
      - "8765:8765"
    volumes:
      - agent_data:/app/data
    networks:
      - vm-net

  # ========== Java 后端 ==========
  video-mind-java:
    build: ./video-mind-java
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://vm-mysql:3306/video_mind?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=123456
      - SPRING_DATA_REDIS_HOST=vm-redis
      - SPRING_RABBITMQ_HOST=vm-rabbitmq
      - SPRING_RABBITMQ_USERNAME=admin
      - SPRING_RABBITMQ_PASSWORD=123456
      - AGENT_SERVICE_URL=http://vm-agent:8765
      - PARSER_SERVICE_URL=http://vm-parser:8001
      - NACOS_SERVER=vm-nacos:8848
    depends_on:
      nacos:
        condition: service_healthy
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
      rabbitmq:
        condition: service_started
      video-mind-parser:
        condition: service_started
      video-mind-agent:
        condition: service_started
    networks:
      - vm-net

  nacos:
    image: nacos/nacos-server:v2.3.0
    container_name: vm-nacos
    environment:
      - MODE=standalone
      - SPRING_DATASOURCE_PLATFORM=mysql
      - MYSQL_SERVICE_HOST=vm-mysql
      - MYSQL_SERVICE_DB_NAME=nacos
      - MYSQL_SERVICE_PORT=3306
      - MYSQL_SERVICE_USER=root
      - MYSQL_SERVICE_PASSWORD=123456
      - MYSQL_SERVICE_DB_PARAM=allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai
    ports:
      - "8848:8848"
      - "9848:9848"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8848/nacos/index.html"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 30s
    restart: unless-stopped
    networks:
      - vm-net

  # ========== Vue + Nginx 前端 ==========
  video-mind-web:
    build: ./video-mind-web
    container_name: vm-web
    ports:
      - "80:80"
    depends_on:
      - video-mind-java
    networks:
      - vm-net

volumes:
  mysql_data:
  agent_data:
  qdrant_data:

networks:
  vm-net:
    driver: bridge
